---
title: "Class Activity: Multiple Regression"
output: 
  html_document:
    theme: cerulean
    code_folding: hide
---

## Class Activity Solutions


```{r}
mt.lm <- lm(mpg ~ qsec + am + qsec:am, data=mtcars)      
summary(mt.lm)
par(mfrow=c(1,2))
plot(mt.lm, which=1:2)
```


```{r}
palette(c("skyblue","firebrick"))
plot(mpg ~ qsec, data=mtcars, col=as.factor(am), pch=16, xlim=c(14,28), main="1974 Motor Trend Cars")
b <- mt.lm$coefficients
abline(b[1], b[2], col=palette()[1])
abline(b[1]+b[3], b[2]+b[4], col=palette()[2])
legend("topright", legend=c("automatic","manual"), col=palette(), pch=1, bty="n", title="Transmission (am)")

```


State the Model:

$$
  Y_i = \beta_0 + \beta_1 \underbrace{X_{1i}}_{qsec} + \beta_2 \underbrace{X_{2i}}_{am} + \beta_3 \underbrace{X_{1i}X_{2i}}_{interaction}  + \epsilon_i
$$

State the Fitted Regression Line:

$$
  \hat{Y}_i = -9.01 + 1.44 X_{1i} - 14.51 X_{2i} + 1.32 X_{1i}X_{2i}
$$






